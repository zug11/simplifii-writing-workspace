# Server-only OpenAI configuration. Never prefix this key with NEXT_PUBLIC_.
OPENAI_API_KEY=
AI_MODEL=gpt-5.6-terra

# One shared early-access code. Server-only; never prefix with NEXT_PUBLIC_.
SIMPLIFII_INVITE_CODE=

# Random server-only signing secret (32+ characters) for expiring invite sessions.
SIMPLIFII_INVITE_SESSION_SECRET=

# Direct tester feedback email via Resend. Keep the API key server-only.
# Verify simplifii.com.au in Resend before using the production sender address.
RESEND_API_KEY=
FEEDBACK_FROM_EMAIL=Simplifii Feedback <feedback@simplifii.com.au>
FEEDBACK_TO_EMAIL=aaron@simplifii.com.au
